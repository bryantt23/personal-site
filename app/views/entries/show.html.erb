<h1><%= @entry.title %></h1>


	<div id="content">
		<%= markdown @entry.body  %>
	</div>

<br>
<br>
<%= render 'disqus' %>


<%= link_to "Edit", edit_entry_path(@entry) %>
<%= link_to "Delete", entry_path(@entry), method: :delete, data: { confirm: "Are you sure you want to delete this post?"} %>
